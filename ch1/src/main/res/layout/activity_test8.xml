<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Test8Activity">

    <!--    화면에 같이 나오는 뷰의 상호 연동한다. 같이 스크롤한다.
    RecyclerView와 AppBarLayout-->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/coordinator"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <!--        화면 상단을 다양하게 구성하기 위해서 AppBarLayout을 사용한다.-->
        <com.google.android.material.appbar.AppBarLayout
            android:id="@+id/appbar"
            android:layout_width="match_parent"
            android:layout_height="242dp"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

            <!--            앱바가 접히는 정책을 설정한다. (접힌다면 어떻게 접힐거니 같은 설정)
            contentScrimsm : 색상을 지정한다. 어떤 색상이냐면..
            appbar가 접히다가 appbar 부분이 정상적으로 나오기 힘들정도로 작아지면, 굳이 내용을 출력하지 말고
            지정한 색상으로 칠하라

            ?attr 현재 앱의 테마에 이 이름으로 연결되어 있는, 지정이 되어 있는 값을 표시해라 의 의미 -> 다크모드에서 유용

            layout_scrollFlags="scroll|exitUntilCollapsed" : 접히고. 한줄은 남기게 된다.
            -->
            <com.google.android.material.appbar.CollapsingToolbarLayout
                android:layout_width="match_parent"
                android:layout_height="242dp"
                app:contentScrim="?attr/colorPrimary"
                app:expandedTitleMarginBottom="50dp"
                app:expandedTitleMarginStart="48dp"
                app:layout_scrollFlags="scroll|exitUntilCollapsed"
                app:title="장원영">

                <!--
                                    app:layout_collapseMode : 앱바에 나오는 개별 뷰들은 앱바가 접힐때 어떻게 할건지 설정할 수 있다.
                                    parallax : 같이 접힌다는 의미
                                    pin : 고정한다.-->
                <ImageView
                    android:id="@+id/appbarImageView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:src="@drawable/baseball"
                    app:layout_collapseMode="parallax" />

                <androidx.appcompat.widget.Toolbar
                    android:id="@+id/toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="?attr/actionBarSize"
                    android:background="#00000000"
                    android:elevation="4dp"
                    android:minHeight="?attr/actionBarSize"
                    app:layout_collapseMode="pin"
                    app:titleTextColor="#ffffff" />

                <com.google.android.material.tabs.TabLayout
                    android:id="@+id/tabs"
                    android:layout_width="match_parent"
                    android:layout_height="50dp"
                    android:layout_gravity="bottom"
                    android:background="#00000000"
                    app:layout_collapseMode="parallax"
                    app:tabMode="scrollable"
                    app:tabTextColor="#ffffff" />

            </com.google.android.material.appbar.CollapsingToolbarLayout>

        </com.google.android.material.appbar.AppBarLayout>

        <!--        화면 본문 구역
        layout_behavior : 단순 문자열 아니다. 클래스 명을 표현하는 문자열이다. 클래스명 AppBarLayout 안에 inner로 선언된 클래스 명이다.
        왜 필요하냐면, CoordinatorLayout을 사용했다고 하더라도 CoordinatorLayout에게 자신의 스크롤 정보를 누구에게 전달해야하는지 알려줘야한다.
        -->
        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/viewPager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:headerLayout="@layout/navigation_header"
        app:menu="@menu/menu_navigation" />

</androidx.drawerlayout.widget.DrawerLayout>